import middleware.ts  getRequestConfig  from "next-intl/server"
export default getRequestConfig(async (middleware.ts  locale ) => (middleware.ts 
  messages: (await import(./messages/$locale.json)).default
))
